name : Smoke testing for infra and networking

on:
  workflow_dispatch

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Smoke test successfully ✅"